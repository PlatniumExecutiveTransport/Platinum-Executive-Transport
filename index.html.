<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Luxury Black Car Service</title>
<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: #000;
        color: #fff;
    }

    /* Gold Color */
    :root {
        --gold: #d4af37;
        --matte-gold: #c4a453;
    }

    header {
        background: #000;
        padding: 20px;
        text-align: center;
        border-bottom: 1px solid var(--gold);
    }

    header h1 {
        font-size: 38px;
        color: var(--gold);
        letter-spacing: 2px;
        margin: 0;
    }

    nav {
        margin-top: 10px;
    }

    nav a {
        color: var(--matte-gold);
        text-decoration: none;
        margin: 0 15px;
        font-size: 16px;
    }

    /* HERO */
    .hero {
        background: url('https://images.unsplash.com/photo-1503376780353-7e6692767b70?auto=format&fit=crop&w=1400&q=80') center/cover no-repeat;
        height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 0 20px;
        position: relative;
    }

    .hero:before {
        content: "";
        position: absolute;
        top: 0; left: 0;
        width: 100%; height: 100%;
        background: rgba(0,0,0,0.6);
    }

    .hero-content {
        position: relative;
        max-width: 650px;
    }

    .hero-content h2 {
        font-size: 48px;
        margin-bottom: 15px;
        color: var(--gold);
    }

    .hero-content p {
        font-size: 20px;
        line-height: 1.6;
    }

    .btn {
        display: inline-block;
        background: var(--gold);
        color: black;
        padding: 12px 30px;
        border-radius: 5px;
        margin-top: 20px;
        font-weight: bold;
        text-decoration: none;
        transition: 0.3s;
    }

    .btn:hover {
        background: var(--matte-gold);
    }

    /* SECTIONS */
    section {
        padding: 60px 20px;
        max-width: 1100px;
        margin: auto;
    }

    h2 {
        text-align: center;
        color: var(--gold);
        font-size: 34px;
        margin-bottom: 40px;
    }

    /* SERVICES */
    .service-box {
        background: #111;
        padding: 30px;
        margin-bottom: 25px;
        border-left: 5px solid var(--gold);
        border-radius: 5px;
    }

    /* FLEET */
    .fleet {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 25px;
    }

    .fleet img {
        width: 100%;
        border-radius: 8px;
        border: 2px solid var(--gold);
    }

    /* BOOKING FORM */
    form {
        background: #111;
        padding: 30px;
        border-radius: 8px;
        border: 1px solid var(--gold);
        max-width: 600px;
        margin: auto;
    }

    input, textarea {
        width: 100%;
        padding: 12px;
        margin-bottom: 15px;
        border: 1px solid var(--matte-gold);
        background: #000;
        color: white;
        border-radius: 5px;
    }

    textarea {
        height: 100px;
    }

    /* FOOTER */
    footer {
        text-align: center;
        padding: 20px;
        color: var(--matte-gold);
        border-top: 1px solid var(--gold);
    }
</style>
</head>
<body>

<header>
    <h1>Elite Black Car Service</h1>
    <nav>
        <a href="#services">Services</a>
        <a href="#fleet">Fleet</a>
        <a href="#booking">Booking</a>
        <a href="#contact">Contact</a>
    </nav>
</header>

<section class="hero">
    <div class="hero-content">
        <h2>Luxury • Professional • On Time</h2>
        <p>Premium black car service for airport transfers, corporate travel, events, and VIP clients. Experience unmatched comfort and professionalism.</p>
        <a class="btn" href="#booking">Book Now</a>
    </div>
</section>

<section id="services">
    <h2>Our Services</h2>

    <div class="service-box">
        <h3>Airport Transfers</h3>
        <p>Reliable pickup and drop-off with real-time flight tracking and professional chauffeurs.</p>
    </div>

    <div class="service-box">
        <h3>Corporate Transportation</h3>
        <p>Dedicated black car service for executives, meetings, conferences, and business travel.</p>
    </div>

    <div class="service-box">
        <h3>Special Events</h3>
        <p>Weddings, birthdays, concerts, proms — arrive in style with premium luxury vehicles.</p>
    </div>
</section>

<section id="fleet">
    <h2>Our Fleet</h2>
    <div class="fleet">
        <img src="https://images.unsplash.com/photo-1617814076516-0d613fdf7c7f?auto=format&fit=crop&w=800&q=80">
        <img src="https://images.unsplash.com/photo-1503736334956-4c8f8e92946d?auto=format&fit=crop&w=800&q=80">
        <img src="https://images.unsplash.com/photo-1590362891991-f776e747a588?auto=format&fit=crop&w=800&q=80">
    </div>
</section>

<section id="booking">
    <h2>Book a Ride</h2>
    <form>
        <input type="text" placeholder="Full Name" required>
        <input type="text" placeholder="Pickup Location" required>
        <input type="text" placeholder="Dropoff Location" required>
        <input type="datetime-local" required>
        <input type="tel" placeholder="Phone Number" required>
        <textarea placeholder="Additional Details"></textarea>
        <button class="btn" type="submit">Submit Booking</button>
    </form>
</section>

<section id="contact">
    <h2>Contact Us</h2>
    <p style="text-align:center; color: var(--matte-gold); font-size: 18px;">
        📞 (555) 555-5555<br>
        ✉️ info@blackcarservice.com<br>
        📍 Serving Cleveland & surrounding areas
    </p>
</section>

<footer>
    © 2025 Elite Black Car Service — All Rights Reserved
</footer>

</body>
</html>
